# Project Rules for Antigravity

- **Git Operations**: Do NOT run `git push` automatically. Always let the user review code locally and perform the push themselves, or ask for explicit confirmation before pushing.
